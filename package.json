{
  "name": "looptube",
  "version": "1.0.0",
  "description": "application to talk about music with friends",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "backend": "cd ./backend && nodemon index.js",
    "frontend": "cd ./client && npm start",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""

  },
  "author": "Jonathan ESPIARD",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.1.0"
  }
}
